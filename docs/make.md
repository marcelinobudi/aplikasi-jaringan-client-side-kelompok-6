# Makefile
## Installation
```bash
sudo dnf install make -y
sudo dnf install cmake -y
```

## Setup 
Run this command only once
```bash
cmake -B build
```

## Compilation
```bash
cmake --build build
```

## Run
```bash
build/client_networks
```